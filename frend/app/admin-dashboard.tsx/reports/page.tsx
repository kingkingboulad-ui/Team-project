export default function AdminReportsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold text-navy-900">Reports</h1>
      <p className="mt-2 max-w-md text-sm text-navy-900/60">
        Not built yet — this is a placeholder so the sidebar link works.
        Wire this up to real reports data once the admin API exists.
      </p>
    </main>
  );
}
